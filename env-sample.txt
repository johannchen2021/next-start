# Database

# Start local Prisma dev instance with: pnpm exec prisma dev

DATABASE_URL=""

# Better Auth

BETTER_AUTH_URL="http://localhost:3000"

# Google OAuth - Get these from https://console.cloud.google.com

# Steps:

# 1. Create a new project in Google Cloud Console

# 2. Enable Google+ API

# 3. Create OAuth 2.0 credentials (Web application)

# 4. Add authorized redirect URIs: http://localhost:3000/api/auth/callback/google

# 5. Copy Client ID and Client Secret below

GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Public URLs (for client-side)

NEXT_PUBLIC_BETTER_AUTH_URL="http://localhost:3000"
